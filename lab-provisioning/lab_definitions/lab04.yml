---
CVP_DEVICES:
  TEAM01-SPINE1:
    name: TEAM01-SPINE1
    parentContainerName: STAGING
    configlets:
        - TEAM01-SPINE1
    imageBundle: []

CVP_CONTAINERS:
  STAGING:
    parent_container: Tenant

CVP_CONFIGLETS:
  GLOBAL-aliases: "{{ lookup('file', 'eos_configs/common.cfg') }}"
  TEAM01-SPINE1: "{{ lookup('file', 'eos_configs/lab02/TEAM01-SPINE1.cfg') }}"